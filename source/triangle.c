#include "triangle.h"

/*
void create_circle_vertices(vec3f *pts);
{
    float theta = 0.0f;
    float step = (2 * PI * r) / npts;
    int n = pts.length();
    for(int i = 0; i < n; i++) {
        pts[i]->x = sin(theta);
        pts[i]->y = cos(theta);
        theta += step;
    }
}
*/

